# Core - everything the default (offline) system needs.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10

# Optional - only needed for the LLM-planned backend (--backend openai|anthropic).
# openai>=1.40
# anthropic>=0.39
